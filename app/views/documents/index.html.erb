<!-- app/views/documents/index.html.erb -->
<h1>Documents</h1>

<% if @documents.any? %>
  <ul>
    <% @documents.each do |doc| %>
      <li><%= link_to doc.title, document_path(doc) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No documents yet — add one from the console or seeds.</p>
<% end %>
